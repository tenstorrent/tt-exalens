name: On PR

on:
  workflow_dispatch:
  pull_request:
    branches: [ "main" ]

jobs:
  static-checks:
    uses: ./.github/workflows/static-checks.yml
    secrets: inherit
  build-and-test:
    uses: ./.github/workflows/build-and-test.yml
    secrets: inherit
