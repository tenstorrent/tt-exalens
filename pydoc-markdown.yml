# This script is used to generate markdown documentation for the python code
# through the pydoc-markdown package:
# pip install pydoc-markdown
# And then in the root directory of the project run:
# pydoc-markdown > docs/library-docs.md
loaders:
  - type: python
    search_path: ["dbd"]
    modules:
      - tt_debuda_lib
      - tt_debuda_init
      - tt_coordinate
# 